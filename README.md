# PostgreSQL Indexes — Talk

Slidev presentation covering PostgreSQL index internals, types, costs, and strategy.

## Requirements

- Node.js 18+

## Run

```bash
npm install
npm run dev
```

Open http://localhost:3030 in your browser.

## Build

```bash
npm run build   # outputs to dist/
npm run export  # exports to PDF
```
